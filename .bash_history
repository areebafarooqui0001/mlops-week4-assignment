ls -la
pip install uv
pip install dvc
uv --python 3.12 .env
uv venv --python 3.12 .env
source .env/bin/activate
ls -larth
pip install uv
pip installl dvc
pip install dvc
ls -la
rm -rf .dvc .dvcignore .env .git .gitconfig
ls -la
ls -larth
pip uninstall dvc
ls -la
rm -rf .dvc .dvcignore .env .git .gitconfig
ls -la
rm -rf .dvc .dvcignore .env .git .gitconfig
ls -la
ls -la
ls -la
pip install uv
uv vevv --python 3.12 .env
uv venv --python 3.12 .env
source .env/bin/activate
uv pip install dvc
git init
dvc init
dvc add data
ls -la
cat .gitignore
ls -larth .dvc
dvc add artifacts/model.joblib
git add artifacts/model.joblib.dvc artifacts/.gitignore data.dvc .gitignore
git commit -m "first commit with 150 datapoints"
git tag -a "model  v1.0, 150 datapoints"
git tag -a "v1.0" -m "model  v1.0, 150 datapoints"
git tag -a "v1.0" -m "model v1.0, 150 datapoints”
git tag -a "v1.0" -m "model v1.0, 150 datapoints"
git config --global user.email "areebafarooqui08@gmail.com"
git config --global user.name "areeba"
git commit -m "first commit with 150 datapoints"
git tag -a "v1.0" -m "model v1.0, 150 datapoints"
catt data/iris.csv | tail -30 >>data/iris.csv
cat data/iris.csv | tail -30 >>data/iris.csv
dvc add data
dvc add artifats/model.joblib
dvc add artifacts/model.joblib
git add artifacts/model.joblib.dvc data.csv
git add artifacts/model.joblib.dvc data.dvc
git commit -m "second iteration with 180 datapoints"
git tag -a "v2.0" -m "model v2.0, 180 datapoints"
git checkout v1.0
dvc checkout
git checkout v2.0
dvc checkout
ls -la
pip install uv
uv venv --python 3.12 .env
source .env/bin/activate
uv pip install dvc
git init
dvc init
dvc add data
ls -la
cat .gitignore
ls -la .dvc
ls -la .dvc/cache
dvc add artifacts/model.joblib
git add artifacts/model.joblib.dvc artifacts/.gitignore data.dvc .gitignore
git commit -m "first iteration with 150 datapoints"
git config --global user.eamil "areebafarooqui08@gmail.com"
git config --global user.name "areeba"
git commit -m "first iteration with 150 datapoints"
git tag -a "v1.0" -m "model v1.0, 150 datapoints"
cat data/iris.csv | head -30 >> data/iris.csv
dvc add data
head -n -30 data/iris.csv > data/iris_fixed.csv
cat data/iris.csv | tail -30 >> data/iris.csv
dvc add data
dvc add artifacts/model.joblib
git add artifacts/model.joblib.dvc data.dvc
git commit -m "second iteration with 180 datapoints"
git tag -a "v2.0" -m "model v2.0, 180 datapoints"
git checkout v1.0
dvc checkout
git checkout v2.0
dvc checkout
git checkout v1.0
dvc checkout
git checkout v2.0
dvc checkout
git clone https://github.com/areebafarooqui0001/mlops-week4-assignment.git
cd mlops-week4-assignment
cd ..
rm mlops-week4-assignment
rm -r mlops-week4-assignment
